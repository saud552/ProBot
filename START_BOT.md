# إعداد وتشغيل البوت

## ✅ الحالة الحالية

- ✓ PHP 8.3.6 مثبت ويعمل
- ✓ الخادم يعمل على المنفذ 8080
- ✓ توكن البوت مضبوط: `7174411191:AAHhRIakJPu0B_9bxMsIlkGfMvRNYsYge7A`
- ✓ قاعدة البيانات موجودة
- ✓ جميع الملفات في مكانها

## 🚀 تشغيل البوت

البوت يعمل حالياً على:
```
http://0.0.0.0:8080
```

## 📡 إعداد Webhook

لإعداد webhook، تحتاج إلى URL عام يمكن لـ Telegram الوصول إليه.

### الطريقة 1: استخدام URL عام
إذا كان لديك domain أو IP عام:
```bash
php setup_webhook.php https://your-domain.com/index.php
```

### الطريقة 2: استخدام Cloudflared
```bash
cloudflared tunnel --url http://localhost:8080
```
ثم استخدم الـ URL المعطى في الأمر أعلاه.

### الطريقة 3: استخدام Ngrok
```bash
ngrok http 8080
```
ثم استخدم الـ URL المعطى في الأمر أعلاه.

## 🔍 التحقق من حالة البوت

```bash
# التحقق من الخادم
ps aux | grep "php -S"

# عرض السجلات
tail -f logs/server.log

# التحقق من webhook
curl "https://api.telegram.org/bot7174411191:AAHhRIakJPu0B_9bxMsIlkGfMvRNYsYge7A/getWebhookInfo"
```

## 📝 ملاحظات

- البوت يستمع على `0.0.0.0:8080` مما يعني أنه متاح من جميع الواجهات
- السجلات تُحفظ في `logs/server.log`
- قاعدة البيانات في `storage/database.sqlite`
