# دليل استخدام ملف التشغيل الشامل
# Comprehensive Start Script Guide

## 📋 نظرة عامة

ملف `start.sh` هو سكريبت شامل يقوم بكل شيء تلقائياً:
- ✅ التحقق من المتطلبات (PHP، الإضافات)
- ✅ إعداد المجلدات والصلاحيات
- ✅ إعداد قاعدة البيانات
- ✅ التحقق من صحة الكود
- ✅ إعداد Webhook (اختياري)
- ✅ تشغيل البوت

---

## 🚀 الاستخدام السريع

### الطريقة 1: تشغيل مباشر
```bash
./start.sh
```

### الطريقة 2: مع Webhook URL
```bash
./start.sh https://your-domain.com/index.php
```

### الطريقة 3: على استضافة مشتركة
```bash
bash start.sh
```

---

## 📝 ما يقوم به السكريبت تلقائياً

### 1. التحقق من المتطلبات
- ✅ PHP 8.1+ مثبت
- ✅ الإضافات المطلوبة: PDO, SQLite, cURL, JSON, mbstring
- ✅ SQLite3 (اختياري)

### 2. إعداد المجلدات
- ✅ `storage/` و `storage/backups/`
- ✅ `logs/`
- ✅ `tmp/`

### 3. إعداد الصلاحيات
- ✅ صلاحيات القراءة/الكتابة على المجلدات
- ✅ صلاحيات قاعدة البيانات
- ✅ صلاحيات ملفات JSON

### 4. إعداد قاعدة البيانات
- ✅ إنشاء قاعدة بيانات جديدة إذا لم تكن موجودة
- ✅ تنفيذ `schema.sql`
- ✅ تنفيذ `seed_settings.sql`
- ✅ إضافة مزود افتراضي إذا لزم الأمر

### 5. التحقق من الكود
- ✅ التحقق من وجود جميع الملفات المطلوبة
- ✅ فحص صحة كود PHP

### 6. خيارات التشغيل
بعد اكتمال الإعداد، يمكنك اختيار:
1. **تشغيل محلي** - PHP Built-in Server
2. **تشغيل مع Cloudflare Tunnel** - للاستضافة بدون SSL
3. **عرض معلومات Webhook** - التحقق من الإعداد الحالي
4. **إعادة إعداد Webhook** - تحديث URL
5. **مراقبة السجلات** - عرض السجلات مباشرة
6. **الخروج**

---

## 🔧 أمثلة الاستخدام

### مثال 1: تشغيل على VPS
```bash
# 1. رفع الملفات إلى الخادم
scp -r * user@server:/var/www/bot/

# 2. الدخول إلى الخادم
ssh user@server
cd /var/www/bot

# 3. تشغيل السكريبت
./start.sh

# 4. اختيار الخيار 1 (تشغيل محلي) أو 2 (مع Cloudflare)
```

### مثال 2: تشغيل على استضافة مشتركة
```bash
# 1. رفع الملفات عبر FTP
# 2. الدخول عبر SSH
cd public_html

# 3. تشغيل السكريبت
bash start.sh

# 4. اختيار الخيار 4 (إعداد Webhook) مع URL الاستضافة
```

### مثال 3: تشغيل مع Webhook تلقائي
```bash
# إذا كان لديك URL جاهز
./start.sh https://your-domain.com/index.php
```

---

## 🌐 خيارات التشغيل التفصيلية

### الخيار 1: تشغيل محلي
```bash
# يشغل PHP Built-in Server على المنفذ 8080
# مناسب للتطوير والاختبار
# الوصول: http://localhost:8080
```

### الخيار 2: تشغيل مع Cloudflare Tunnel
```bash
# يشغل PHP Server + Cloudflare Tunnel
# يحصل على URL عام مثل: https://xxx.trycloudflare.com
# مناسب للاستضافة بدون SSL
# يقوم بتثبيت cloudflared تلقائياً إذا لزم الأمر
```

### الخيار 3: عرض معلومات Webhook
```bash
# يعرض معلومات Webhook الحالية من Telegram API
# مفيد للتحقق من الإعداد
```

### الخيار 4: إعادة إعداد Webhook
```bash
# يطلب منك إدخال URL جديد
# يقوم بإعداد Webhook تلقائياً
```

### الخيار 5: مراقبة السجلات
```bash
# يعرض السجلات مباشرة (tail -f)
# مفيد للمراقبة والتصحيح
```

---

## ⚙️ المتغيرات البيئية

يمكنك تعديل بعض الإعدادات عبر المتغيرات:

```bash
# تغيير منفذ الخادم
PORT=3000 ./start.sh

# تشغيل في الخلفية
nohup ./start.sh > /dev/null 2>&1 &
```

---

## 🐛 استكشاف الأخطاء

### خطأ: "PHP غير مثبت"
```bash
# Ubuntu/Debian
sudo apt-get install php php-cli php-sqlite3 php-curl php-mbstring

# CentOS/RHEL
sudo yum install php php-cli php-pdo php-sqlite php-curl php-mbstring

# macOS
brew install php
```

### خطأ: "إضافات PHP مفقودة"
```bash
# Ubuntu/Debian
sudo apt-get install php-sqlite3 php-curl php-mbstring

# CentOS/RHEL
sudo yum install php-pdo php-sqlite php-curl php-mbstring
```

### خطأ: "صلاحيات غير كافية"
```bash
# تأكد من صلاحيات المجلدات
chmod 755 storage logs tmp
chmod 644 storage/database.sqlite
```

### خطأ: "قاعدة البيانات لا تعمل"
```bash
# حذف قاعدة البيانات القديمة وإعادة الإنشاء
rm storage/database.sqlite
./start.sh
```

---

## 📊 المخرجات

السكريبت يعرض:
- ✅ رسائل نجاح (باللون الأخضر)
- ⚠️ تحذيرات (باللون الأصفر)
- ✗ أخطاء (باللون الأحمر)
- ℹ️ معلومات (باللون الأزرق)

---

## 🔒 الأمان

- ✅ السكريبت لا يطلب كلمات مرور
- ✅ جميع العمليات محلية
- ✅ لا يتم إرسال بيانات حساسة
- ✅ Webhook URL فقط يتم إرساله إلى Telegram API

---

## 📞 الدعم

في حالة وجود مشاكل:
1. تحقق من السجلات: `logs/server.log`
2. تحقق من صلاحيات الملفات
3. تأكد من تثبيت جميع المتطلبات
4. راجع `DEPLOYMENT_GUIDE.md` للتفاصيل

---

## 🎯 نصائح

1. **للإنتاج**: استخدم Apache/Nginx بدلاً من PHP Built-in Server
2. **للاختبار**: استخدم Cloudflare Tunnel للوصول السريع
3. **للمراقبة**: استخدم `monitor.sh` أو الخيار 5 في السكريبت
4. **للنسخ الاحتياطي**: استخدم `scripts/backup_database.php`

---

## 📝 ملاحظات

- السكريبت يعمل على Linux و macOS
- على Windows، استخدم WSL أو Git Bash
- تأكد من وجود صلاحيات تنفيذ: `chmod +x start.sh`
- السكريبت يتطلب اتصال بالإنترنت لإعداد Webhook
